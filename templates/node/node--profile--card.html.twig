
{# The card and list-item view modes render the same reference card and differ
   only in heading level, so the markup lives in the shared partial (#1648). #}
{% include '@atomic/node/_profile-reference-card.html.twig' with {
  default_heading_level: '2',
} %}
